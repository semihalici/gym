@model IEnumerable<Gym_odev.Models.Egitmen>

@{
    ViewData["Title"] = "Eğitmenler";
}

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h2>🏋️‍♂️ Eğitmen Listesi</h2>
        <a asp-action="Create" class="btn btn-success">+ Yeni Eğitmen Ekle</a>
    </div>

    <table class="table table-bordered table-striped shadow-sm">
        <thead class="table-dark">
            <tr>
                <th>Ad Soyad</th>
                <th>Uzmanlık Alanı</th>
                <th>İşlemler</th>
            </tr>
        </thead>
        <tbody>
            @if (!Model.Any())
            {
                <tr>
                    <td colspan="3" class="text-center">Henüz kayıtlı eğitmen yok.</td>
                </tr>
            }
            else
            {
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.AdSoyad</td>
                        <td><span class="badge bg-info text-dark">@item.Uzmanlik</span></td>
                        <td>
                            <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-danger" 
                               onclick="return confirm('Silmek istediğine emin misin?');">Sil</a>
                        </td>
                    </tr>
                }
            }
        </tbody>
    </table>
</div>